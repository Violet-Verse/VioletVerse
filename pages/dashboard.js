import { Container } from "@mui/material";

const Dashboard = () => {
    return (
        <div>
            <Container maxWidth="xs">
                <h1>Dashboard</h1>
            </Container>
        </div>
    );
}
};

export default Dashboard;
