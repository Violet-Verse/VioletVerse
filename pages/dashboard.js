import { Grid } from "@mui/material";

const Dashboard = () => {
    return (
        <div>
            <Grid container>
                <h1>Dashboard</h1>
            </Grid>
        </div>
    );
};

export default Dashboard;
